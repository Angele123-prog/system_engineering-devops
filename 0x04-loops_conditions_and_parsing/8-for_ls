#!/usr/bin/env bash
# Display only part of filename after first dash

for file in *-*; do
    # Skip if file starts with a dot (hidden file)
    [ "${file:0:1}" = "." ] && continue
    # Only print the part after the first dash
    echo "${file#*-}"
done
