#!/usr/bin/env bash
# This script lists non-hidden files/directories in the current directory, showing only the part after the first dash.

for file in *; do
  if [[ -f "$file" || -d "$file" ]]; then
    name="${file#*-}"
    if [[ "$file" != "$name" ]]; then
      echo "$name"
    fi
  fi
done

