#!/usr/bin/env bash
#

for file in *; do
    # Skip hidden files
    if [[ "$file" == .* ]]; then
        continue
    fi

    # If file has a dash, print the part after the first dash
    if [[ "$file" == *-* ]]; then
        echo "${file#*-}"
    fi
done
